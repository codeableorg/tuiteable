<aside class="w-1/5 px-5 py-2">
  <div class="bg-gray-800 p-2">
    <% if user_signed_in? %>
      <h2 class="text-lg font-bold my-2 text-center">New features coming soon!</h2>
      <div class="w-full"><%= image_tag "features_soon.png", class: 'mx-auto' %></div>
    <% else %>
      <h2 class="text-lg font-bold my-2 text-center">Log in to {tuiteable} </h2>
      <%= render 'devise/sessions/login' %>
    <% end %>
  </div>
</aside>