<%= render 'layouts/navigation', title: 'Explore', to_link: root_path, arrow: false %>
<% if user_signed_in? %>
  <%= render 'layouts/input_tweet', tweet: Tweet.new, button_text: 'tuiteable!' %>
<% end %>
<%Tweet.where(parent_id: nil).order(created_at: :desc).each do |tweet|%>
  <%= render "pages/tweet", tweet: tweet %>
<%end%>
